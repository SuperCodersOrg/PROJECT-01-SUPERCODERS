- Date : 24 June 2026 (Session 3)
Duration: 3 Hrs 30 mins 
Goal():
To implement hashing function ,understanding hashing codes exploring all edges cases etc.
What I Tried():
I tried to create the hash function for primitive as well as fot the non primitive datatypes 

During the hash function implementation I saw functions that are required to be added to the Linked list.

The first function was 
Findif() - Without Finfid() every time to check th ekey is present in bucket we need to traverse which makes the code duplicated various times thats why i have added findif in it once the logic is written and it can be called anytime.
remove if()- Removeif() works as same as find if i. nstead of finding a node it just nemoves the first node which matched the given condition

DynamicArray(size_t count); it is called as the size constructor whenever it is called it creates the array with the count elements.
const t& opetator[] This is required because whever we have arr constant and another arr it will not be able to chosse the correct overload what all are rewuored thats why this method helps the compiler to do the correct overload.

Outcome():
Modified changes in the Dynamic array and linked list as per the requiremnet in the hashmap implementation.
